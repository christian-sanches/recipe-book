# Dev override — skip Caddy (no SSL needed locally) and force dev mode
services:
  caddy:
    profiles: ["disabled"]

  app:
    environment:
      DEV_MODE: "true"
      NEXTAUTH_URL: "http://192.168.68.150:3000"
    ports:
      - "3000:3000"
